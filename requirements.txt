flask>=3.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
apscheduler>=3.10.4
supabase>=2.4.0
google-generativeai>=0.5.0

# Scrapling — replaces Selenium as the fetch/anti-bot engine (see scraper.py).
# "fetchers" extra pulls in Playwright/Patchright + the stealth fingerprinting deps
# needed by StealthyFetcher/StealthySession. Browsers themselves are installed
# separately via `scrapling install` (see Dockerfile) — pip alone won't fetch them.
scrapling[fetchers]>=0.4.15
